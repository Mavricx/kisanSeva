
            
            <!DOCTYPE html>
            <html lang="en">
            <head>
                <meta charset="UTF-8">
                <meta name="viewport" content="width=device-width, initial-scale=1.0">
                <title>Farmer Login</title>
                <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
                <style>
                    body {
                        background: linear-gradient(45deg, #ff7e5f, #feb47b,  #a8c0ff);
                        background-size: 400% 400%;
                        animation: gradientBackground 15s ease infinite;
                        font-family: 'Roboto', 'Times New Roman', Times, serif;
                        margin: 0;
                        display: flex;
                        justify-content: center;
                        align-items: center;
                        height: 100vh;
                    }
            
                    @keyframes gradientBackground {
                        0% { background-position: 0% 50%; }
                        50% { background-position: 100% 50%; }
                        100% { background-position: 0% 50%; }
                    }
            
                    .container {
                        max-width: 500px;
                        padding: 40px;
                        background-color: rgba(255, 255, 255, 0.9);
                        border-radius: 10px;
                        box-shadow: 0px 15px 20px rgba(0, 0, 0, 0.1);
                        animation: fadeIn 1s ease-in-out;
                    }
            
                    @keyframes fadeIn {
                        from { opacity: 0; transform: translateY(-20px); }
                        to { opacity: 1; transform: translateY(0); }
                    }
            
                    .form-title {
                        text-align: center;
                        font-size: 24px;
                        font-weight: bold;
                        margin-bottom: 30px;
                        animation: scaleIn 0.8s ease-in-out;
                    }
            
                    @keyframes scaleIn {
                        from { transform: scale(0.8); opacity: 0; }
                        to { transform: scale(1); opacity: 1; }
                    }
            
                    input, button {
                        transition: all 0.3s ease;
                    }
            
                    input:focus {
                        border-color: #4CAF50;
                        box-shadow: 0 0 5px rgba(76, 175, 80, 0.8);
                        outline: none;
                        transform: scale(1.02);
                    }
            
                    .btn-custom {
                        background-color: #4CAF50;
                        color: white;
                        border: none;
                        width: 100%;
                        padding: 12px;
                        border-radius: 5px;
                        font-size: 16px;
                        cursor: pointer;
                        animation: bounceIn 0.6s ease;
                    }
            
                    .btn-custom:hover {
                        background-color: #45a049;
                        transform: scale(1.05);
                    }
            
                    @keyframes bounceIn {
                        from { transform: scale(0.9); opacity: 0.7; }
                        to { transform: scale(1); opacity: 1; }
                    }
            
                    .hidden {
                        display: none;
                    }
            
                    .image-container {
                        display: flex;
                        justify-content: center;
                        margin-bottom: 20px;
                    }
            
                    .image-container img {
                        max-width: 100%;
                        height: auto;
                        border-radius: 10px;
                        animation: float 4s ease-in-out infinite;
                    }
            
                    @keyframes float {
                        0% { transform: translatey(0px); }
                        50% { transform: translatey(-10px); }
                        100% { transform: translatey(0px); }
                    }
                </style>
            </head>
            <body>
            <div class="container">
                <div class="image-container">
                    <img src="https://gifdb.com/images/high/farmer-planting-under-the-rain-mhmnqfzyhnjwmj5t.gif" alt="Farmer Image">
                </div>
                <h2 class="form-title">Farmer Login</h2>
                <div class="form-group">
                    <label for="loginAadhar">Aadhar Number</label>
                    <input type="text" class="form-control" id="loginAadhar" placeholder="Enter Aadhar Number" required>
                </div>
                <div class="form-group">
                    <label for="loginPassword">Password</label>
                    <input type="password" class="form-control" id="loginPassword" placeholder="Enter Password" required>
                </div>
                <br>
                <div class="form-footer">
                    <button type="button" class="btn-custom" id="loginBtn" onclick="window.location.href='/dashboard'">Login</button>
                    <br><br>
                    <button type="button" class="btn-custom" onclick="window.location.href='/register'">Register</button>
                </div>
            </div>
            
            <script>
                document.getElementById('loginBtn').addEventListener('click', function () {
                    var loginAadhar = document.getElementById('loginAadhar').value.trim();
                    var loginPassword = document.getElementById('loginPassword').value.trim();
            
                    var farmerData = JSON.parse(localStorage.getItem(loginAadhar));
                    if (farmerData && farmerData.password === loginPassword) {
                        alert('Login Successful! Redirecting to Dashboard...');
                        window.location.href = '/dashboard';
                    } else {
                        alert('Invalid Aadhar Number or Password.');
                    }
                });
            </script>
            
            </body>
            </html>